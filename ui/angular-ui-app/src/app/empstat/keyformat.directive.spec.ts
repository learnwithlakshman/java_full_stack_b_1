import { KeyformatDirective } from './keyformat.directive';

describe('KeyformatDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyformatDirective();
    expect(directive).toBeTruthy();
  });
});
